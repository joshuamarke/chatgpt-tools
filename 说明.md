# ChatGPT Tools 说明

目录：`E:\demo\chatgpt-tools`（仓库路径可不变）

产品名 **ChatGPT Tools**：Tauri 2 轻量壳 + **多功能工具箱**。当前包含：换肤引擎 **v2.3.5**（主路径 apply/status/restore/pause/resume/import/export 进程内 Rust；**三层皮肤模型**）；**会话管理 Phase 2**（侧栏入口：列表/导出 Markdown/删除可撤销/provider 修复/session_index 清理）。皮肤不是唯一主线，规划见 `docs/architecture/features.md`。

```powershell
cd E:\demo\chatgpt-tools
npm install
npm run test:engine
npm run verify:engine
npm run dev
```

## 会话管理（摘要）

- 侧栏 **会话管理** → 分页列表本地 thread；单删 / 多选删除（可撤销）；行内导出 Markdown。  
- 工具条：历史 provider 修复 · `session_index.jsonl` 孤儿预览/应用清理。  
- 数据源：`$CODEX_HOME` 或 `~/.codex` 下 SQLite + rollout + session_index。  
- 删除备份：`%LOCALAPPDATA%\ChatGPTTools\session-backups\`；修复/索引备份：`{CODEX_HOME}/backups_state/provider-sync/`。  
- 代码：`src-tauri/src/sessions/` · `src/features/sessions/` · `window.sessionAPI`。  
- 详情：`docs/features/sessions.md`。

## 引擎 v2.3 摘要

- **共享渲染**：`engine/runtime/renderer-core.js`；皮肤差异在 `assets/plugin.json` 与 CSS。  
- **大尺寸立绘**：允许高质量原图（硬限 16MB / 16384px / 50MP）。先注入 CSS shell，再异步贴立绘；CDP 超时随体积放大。列表缩略图请用 `assets/screenshot.*`。  
- **宿主生命周期**：`offline` / `starting` / `ready`。进程、CDP 端口、`app://` 页三信号综合判断，避免慢电脑启动时误报「未打开」。  
- **两阶段注入**：shell → soft 成功；`applyArt` 贴图。  
- **长驻 injector 热换（2.3）**：watch 进程读 `injector.control.json`（`switch` / `reapply` / `ping`），跨皮肤 **不 stop/spawn**。  
- **slim core 常驻 + delta（2.3）**：`window.__CHATGPT_TOOLS_SKIN_HOST__`；二次换肤优先 `deltaShell`（只推 CSS/markers/plugin），不重传 core。  
- **CDP**：loopback、Browser 身份、pause 文件、stop 身份校验。  
- **生命周期**：async 互斥锁；`applyMode`: `cold` | `hot-switch` | `hot-reapply`；`pause`/`resume`。  
- **2.3.3 稳定性**：pause = 写 flag + 停 keep + **即时 CDP remove**（remove 失败不报完整成功）；stop injector 身份不明不杀；`config.toml` 严格 UTF-8 + 原子写 + 拒 `[desktop.*]` 子表。  
- **三层皮肤模型**：**框架**（core + immersive 基线）提供自适应全窗壁纸/原生控件/建议卡能力 → **契约**是制作约定（遵守即可不破坏上述能力）→ **个性化**由皮肤在 `skins/<id>` 自己实现。payload：**框架基线在前，皮肤 CSS 在后**；引擎不做样式强制覆盖。详见 `docs/development/skin-contract.md`。  
- 慢机器可设环境变量：`CODEX_SKIN_SLOW_SCALE=2`。

> 若曾用旧引擎应用过皮肤，切换失败请先 `restore` 再重新 apply。

## 故障排查

| 现象 | 原因 | 处理 |
|------|------|------|
| 日志 `Invalid --browser-id` | 旧 state 整串当 id | 已规范化；重新 apply |
| 界面只变主题但立绘慢/无 | 大图仍在 `artPending` | 等几秒或看 status.artOk；检查 injector 日志 |
| GUI「未打开」但 ChatGPT 在启动 | 旧逻辑只看进程 | 2.2 应显示「启动中…」；仍误报则设 `CODEX_SKIN_SLOW_SCALE=2` |
| 引擎未就绪 | 主路径换肤/导入导出无需 Node；自定义皮肤或强制 Node 回退需要 Node | 看 `%LOCALAPPDATA%\ChatGPTTools\diag.log`；或 `CODEX_SKIN_FORCE_NODE=1` 对照 |
| 刷新后掉肤 | keep 线程未启动或 CDP 短暂不可用 | 再 apply 一次；看 diag 是否有 `keep: re-inject` |
| 想强制全程 Node | 调试对比 | `CODEX_SKIN_FORCE_NODE=1` 或 `CODEX_SKIN_NATIVE=0` |
| 换肤超时 | 冷启动过慢 | 勾选自动重启；完全退出后再 apply |

## 验证

| 命令 | 作用 |
|------|------|
| `npm run test:engine` | 元数据 / payload / host-probe / injector self-test |
| `npm run verify:engine` | CLI 冒烟 |
| `npm run engine -- check-payload --skin-id dream` | 载荷体积（`recommended` 仅为软提示） |
| `npm run engine -- status` | 查看 `lifecycle` / `shellOk` / `artOk` |

## 文档

| 文档 | 内容 |
|------|------|
| [docs/architecture/overview.md](docs/architecture/overview.md) | 架构与模块边界 |
| [docs/architecture/engine-cli.md](docs/architecture/engine-cli.md) | CLI 协议 |
| [docs/development/create-skin.md](docs/development/create-skin.md) | 新建皮肤 |
| [docs/development/migration.md](docs/development/migration.md) | 迁移与性能说明 |

状态目录：`%LOCALAPPDATA%\ChatGPTTools\`
